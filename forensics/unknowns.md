# Static Reconstruction Unknowns

Unknowns are never filled with convenient behavior. The versioned, machine-readable authority
is [`fidelity/residual-gap-ledger.json`](./fidelity/residual-gap-ledger.json), generated from
[`reference/fidelity-metric-v1.json`](../reference/fidelity-metric-v1.json). It records every
current inference, unknown, compatibility/safety exception, declared platform divergence,
rights decision, custody blocker, and release blocker together with its score effect.

## Current technical unknowns

| Area | Current boundary | Treatment |
|---|---|---|
| Original runtime | No executing legacy runtime, pixel golden, waveform capture, or trajectory trace exists | The fidelity metric measures recovered-contract conformance only; original-runtime identity stays explicitly false |
| Visual output | Exact legacy font rasterization and default-anchor cases without an explicit native setter are not observable | Preserve exact font bytes; use the disclosed default-center inference; do not award recovered credit for either boundary |
| Audio output | Legacy mixer latency/gain/waveform output is unavailable; ten consumed audio resources lack an exact event linkage | Preserve and classify every cue; do not invent event timing |
| Basic blade material | Texture, vertex layout, topology, width, and lifecycle are recovered; numeric blend/sampler state is not | Use the reviewed Creator unlit-texture compatibility material and list the inference separately |
| Classic scheduling | Classic bomb scheduler ownership and one lower-bound side effect are unproven | Do not substitute the fruit-miss path or count the behavior as recovered |
| Crazy Bird | Exact native combo callback operand/order remains ambiguous | Keep the bounded implementation labeled as a reviewed inference |
| RNG | Exact same-frame interleaving with legacy engine/VFX `lrand48` consumers is unavailable | Preserve recovered gameplay consumer order; do not claim an original sequence |
| Physics ray order | Box2D callback order is not a nearest-first contract | Preserve measured forward results followed by reverse results without sorting or deduplication |
| Electric contact | Native `PreSolve` uses an incompatible object layout | Preserve recovered filters/activation with type-safe Creator handling; never reproduce memory-unsafe access |
| Leaf/rope solvers | Exact legacy solver/RNG/frame sampling and framebuffer trajectories are not observable | Preserve the recovered separate boundaries and disclosed compatibility implementations |

## External closeout blockers

- Two independently held offline APK backup copies have not been supplied for hash verification.
- Public-distribution evidence is absent for code, 784 PNG, 62 audio files, 16 fonts, the
  Pencil Blade identity, and the generated Cocos Web runtime.
- `Fonts/CooperBlackStd.otf` needs an explicit authorized exclude/replace/convert decision.
- GitHub authentication, protected runner/branch settings, the `github-pages` environment,
  Pages source, and production deployment remain unavailable while the rights gate is closed.

These blockers do not alter the technical fidelity numerator. They keep evidence-custody and
public-release completion closed.

## Resolved technical facts

- All six production modes, 52 objectives, selector/progression state, audio preload order,
  settings/storage behavior, and gameplay/result lifecycle owners have contract and test
  coverage.
- The canonical resource denominator is frozen at 862 assets. Every entry is byte-verified,
  staged, has captured Creator metadata/UUID, and has either exact consumers or a reviewed
  excluded/unsupported disposition.
- The selected Cocos Box2D backend passes measured trajectory, forward/reverse raycast,
  bilateral contact filtering, world-lock rejection, and deferred-destruction probes.
- Android arm64 API 33 and Chrome 150 at `480x800` and `720x1280` pass input, graphics/audio,
  storage, lifecycle, orientation, and offline runtime checks.
- The five-domain metric uses a minimum-domain rule with no weighting; every domain passes
  `100.00%` of its frozen recovered-contract units. Residuals never add recovered credit.
